import { describe, expect, it } from "vitest";

describe("WorkoutPlan", () => {
  it.todo(
    "não deve permitir ativar um plano sem nenhum WorkoutDay (WorkoutPlan entity to be implemented)"
  );
});
